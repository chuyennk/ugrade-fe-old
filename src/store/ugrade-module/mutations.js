export function setLoginInfo(state, loginInfo) {
  state.loginInfo = loginInfo;
}

export function setSearchResult(state, searchResult) {
  state.searchResult = searchResult;
}

export function setCustInfo(state, custInfo) {
  state.custInfo = custInfo;
}

export function setFssData(state, fssData) {
  state.fssData = fssData;
}

export function setScData(state, scData) {
  state.scData = scData;
}

export function setScList(state, scList) {
  state.scList = scList;
}
